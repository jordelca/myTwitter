{% extends '::layout.html.twig' %}

{% block inner_content %}
    <h1>Twit</h1>

    {% include 'AppBundle:Twit/Includes:twit.html.twig' with {'twit' : entity} %}

        <ul class="record_actions">
    <li>
        <a href="{{ path('twit') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('twit_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
